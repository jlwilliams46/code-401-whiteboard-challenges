{
  "name": "whiteboard-challenge-06",
  "version": "1.0.0",
  "description": "&nbsp; Write a recursive function called loop that has the function signature (count, callback) => undefined It should call the callback count times (count is expected to be > 0)",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint-plugin-jquery": "^1.3.0",
    "jest": "^22.1.4"
  },
    "scripts": {
    "test": "jest --verbose"
  }
}
